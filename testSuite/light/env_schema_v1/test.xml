<testsuite>
  <testcase owner="MoonRay Houdini">
    <description>
    Houdini USD 24 DomeLight_1 schema alias should be accepted as a MoonRay EnvLight.
    </description>
    <commands>
      <command>
        <executable>hd_render</executable>
        <args>-in ${shot_dir}/scene.usd -out ${result} ${args} -res ${res} -renderer Moonray -camera camera -size 256 144 -set 'moonray:sceneVariable:pixel_samples' 2 -set 'moonray:sceneVariable:max_depth' 0</args>
      </command>
      <command>
        <executable>${oiiotool_path}oiiotool</executable>
        <args>${shot_tmp_dir}/${result} ${shot_tmp_dir}/${canonical} -a --warn ${error_threshold} --fail ${error_threshold} --failpercent .01 --hardfail 0.1 --diff --absdiff -o ${shot_tmp_dir}/${diff}</args>
      </command>
    </commands>
  </testcase>
</testsuite>
